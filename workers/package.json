{
	"name": "stw-api",
	"version": "0.1.0",
	"private": true,
	"description": "Edge API + D1 fallback for Sanskrit-Tibetan Workspace v2 (Phase 5)",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"types": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20240725.0",
		"typescript": "^5.5.4",
		"wrangler": "^4.0.0"
	}
}
